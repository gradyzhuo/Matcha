#!/bin/bash -l
#
# matcha
#
# Matcha 1.0, Created by gradyzhuo, enoch_lee.
# Copyright © 2017, Matcha Inc. All rights reserved.
#

if [[ "$BASH_SOURCE" == *"./"* ]]; then
  MATCHA_BASE=$(pwd)
elif [[ -z $MATCHA_BASE ]]; then
  #從執行 shell 路徑判定builder路徑
  declare EXEC_PATH=$(readlink "$BASH_SOURCE")
  declare EXEC_DIR=$(dirname "$EXEC_PATH")


  cd "$EXEC_DIR" >> /dev/null
  MATCHA_BASE=$(pwd)
  cd - >> /dev/null
fi

source "$MATCHA_BASE/.prefix" >> /dev/null
MODULES_FOLDER="$MATCHA_BASE/modules"
source "$MODULES_FOLDER/@.imports" >> /dev/null

@import Prints --silent

print -c green "executing from $MATCHA_BASE"

declare -a array=($@)
if [[ "${array[@]}" =~ "-ci" ]]; then
  BUILD_CI=0
elif [[ "${array[@]}" =~ "-debug" ]]; then
  DEBUG_MODE=0
fi

if [[ "$1" != "" ]]; then
  if [[ $(ls "$MATCHA_BASE/exec") == *"$1"*  ]]; then
    @exec "$@"
  else
    print -c 'red' -s 'bold' "Command \`$1\` not found."
  fi
elif [[ "$0" == "-bash" ]]; then
  welcome

  print -c "green" "============================"
  print -c "green" -s "bold" "Now you can \`@import module\` module to do something."
  print -c "green" -s "bold" 'Try it!\n```\n    @import Prints2\n    print -c "blue" "Hello Matcha!"\n```'
  prints "-c green -s bold 'And you will see the result: '" "-c blue 'Hello Matcha!'"
  print -c green -s bold "Enjoy it!"
else
  welcome
fi
